import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
    palette: {
        primary: {
            main: '#E53935', 
        },
        secondary: {
            main: '#FFB300', 
        },
        accent: {
            main: '#00ACC1', 
        },
    },
});

export default Theme;